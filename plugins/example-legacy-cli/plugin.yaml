# run with:
#   go run ./cmd/helm example-cli-legacy foo bar --baz=qux quxx
name: example-legacy-cli
version: 0.1.0
usage: an example legacy CLI plugin type
description: |
    longer help text
    can be multiline
platformCommand:
  - command: ${HELM_PLUGIN_DIR}/run.sh
